name: Release

on:
  push:
    tags: [ 'v*' ]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-windows:
    name: Build Windows x86_64 (${{ matrix.edition }})
    runs-on: windows-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        edition: [ admin, member ]
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v5

      - name: Setup Rust 1.95
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: 1.95
          target: x86_64-pc-windows-msvc
          cache: false
          rustflags: ''

      - name: Setup protoc
        uses: arduino/setup-protoc@v3
        with:
          version: '25.5'
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Resolve edition vars
        id: vars
        run: |
          set -euo pipefail
          if [ "${{ matrix.edition }}" = "member" ]; then
            echo "features=--no-default-features --features member,mimalloc" >> "$GITHUB_OUTPUT"
            echo "stage=pactmesh-member-windows-x86_64" >> "$GITHUB_OUTPUT"
            echo "setup=pactmesh-member-setup-x86_64" >> "$GITHUB_OUTPUT"
          else
            echo "features=--features mimalloc" >> "$GITHUB_OUTPUT"
            echo "stage=pactmesh-windows-x86_64" >> "$GITHUB_OUTPUT"
            echo "setup=pactmesh-setup-x86_64" >> "$GITHUB_OUTPUT"
          fi

      - name: Build
        run: cargo build --profile ci-artifact --target x86_64-pc-windows-msvc -p pactmesh ${{ steps.vars.outputs.features }} --bin pactmesh --bin pactmesh-core

      - name: Package
        run: |
          set -euo pipefail
          stage="${{ steps.vars.outputs.stage }}"
          mkdir -p "$stage"
          cp target/x86_64-pc-windows-msvc/ci-artifact/pactmesh.exe "$stage"/
          cp target/x86_64-pc-windows-msvc/ci-artifact/pactmesh-core.exe "$stage"/
          cp pactmesh/third_party/x86_64/*.dll "$stage"/
          cp pactmesh/third_party/x86_64/*.sys "$stage"/
          cp script/install.ps1 script/install.cmd "$stage"/
          cp README.md "$stage"/ || true
          7z a -tzip "$stage".zip "$stage" >/dev/null

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.vars.outputs.stage }}
          path: ${{ steps.vars.outputs.stage }}.zip
          if-no-files-found: error

      - name: Build offline installer (Inno Setup)
        run: |
          set -euo pipefail
          choco install innosetup --no-progress -y
          ver="${GITHUB_REF_NAME#v}"
          case "$ver" in ''|[!0-9]* ) ver="0.0.0" ;; esac
          iscc="/c/Program Files (x86)/Inno Setup 6/ISCC.exe"
          "$iscc" \
            "//DAppVersion=${ver}" \
            "//DEdition=${{ matrix.edition }}" \
            "//DSourceDir=${GITHUB_WORKSPACE}\\${{ steps.vars.outputs.stage }}" \
            "//DOutDir=${GITHUB_WORKSPACE}" \
            script/pactmesh.iss
          ls -la "${{ steps.vars.outputs.setup }}.exe"

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.vars.outputs.setup }}
          path: ${{ steps.vars.outputs.setup }}.exe
          if-no-files-found: error

  build-linux:
    name: Build Linux x86_64 musl (${{ matrix.edition }})
    runs-on: ubuntu-22.04
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        edition: [ admin, member ]
    steps:
      - uses: actions/checkout@v5

      - name: Install protoc
        run: |
          PROTOC_VERSION=25.5
          curl -LsSf -o /tmp/protoc.zip \
            https://github.com/protocolbuffers/protobuf/releases/download/v${PROTOC_VERSION}/protoc-${PROTOC_VERSION}-linux-x86_64.zip
          unzip -q /tmp/protoc.zip -d /tmp/protoc25
          sudo install -m 0755 /tmp/protoc25/bin/protoc /usr/local/bin/protoc
          sudo cp -r /tmp/protoc25/include/google /usr/local/include/
          protoc --version

      - name: Install build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends libclang-dev clang pkg-config musl-tools

      - name: Setup Rust toolchain (rust-toolchain.toml pins 1.95)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-musl

      - name: Add musl target for pinned toolchain
        run: rustup target add x86_64-unknown-linux-musl

      - name: Resolve edition vars
        id: vars
        run: |
          set -euo pipefail
          if [ "${{ matrix.edition }}" = "member" ]; then
            echo "features=--no-default-features --features member" >> "$GITHUB_OUTPUT"
            echo "stage=pactmesh-member-linux-x86_64" >> "$GITHUB_OUTPUT"
          else
            echo "features=" >> "$GITHUB_OUTPUT"
            echo "stage=pactmesh-linux-x86_64" >> "$GITHUB_OUTPUT"
          fi

      - name: Build (static musl)
        run: cargo build --release --target x86_64-unknown-linux-musl -p pactmesh ${{ steps.vars.outputs.features }} --bin pactmesh --bin pactmesh-core

      - name: Package
        run: |
          set -euo pipefail
          stage="${{ steps.vars.outputs.stage }}"
          bindir=target/x86_64-unknown-linux-musl/release
          mkdir -p "$stage"
          cp "$bindir"/pactmesh "$stage"/
          cp "$bindir"/pactmesh-core "$stage"/
          cp script/install.sh "$stage"/
          cp README.md "$stage"/ || true
          chmod +x "$stage"/pactmesh "$stage"/pactmesh-core "$stage"/install.sh
          tar -czf "$stage".tar.gz "$stage"

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.vars.outputs.stage }}
          path: ${{ steps.vars.outputs.stage }}.tar.gz
          if-no-files-found: error

  release:
    name: Publish GitHub Release
    needs: [ build-windows, build-linux ]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5

      - uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/pactmesh-windows-x86_64/pactmesh-windows-x86_64.zip
            dist/pactmesh-member-windows-x86_64/pactmesh-member-windows-x86_64.zip
            dist/pactmesh-setup-x86_64/pactmesh-setup-x86_64.exe
            dist/pactmesh-member-setup-x86_64/pactmesh-member-setup-x86_64.exe
            dist/pactmesh-linux-x86_64/pactmesh-linux-x86_64.tar.gz
            dist/pactmesh-member-linux-x86_64/pactmesh-member-linux-x86_64.tar.gz
            script/install.sh
            script/install.ps1
          generate_release_notes: true
          fail_on_unmatched_files: true
